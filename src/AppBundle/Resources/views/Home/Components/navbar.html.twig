<header id="header" class="clearfix">
    {# navbar #}
    <nav class="navbar navbar-default">
        <div class="container">
            {# navbar-header #}
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
                        data-target="#navbar-collapse">
                    <span class="sr-only">{{ 'navigation'|trans }}</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="#">AutoSkautas.lt</a>
            </div>
            {# /navbar-header #}

            <div class="navbar-left">
                <div class="collapse navbar-collapse" id="navbar-collapse">
                    <ul class="nav navbar-nav">
                        <li class="active"><a href="#">{{ 'find_car'|trans }}</a></li>
                        <li><a href="#">Kaip tai veikia?</a></li>
                        <li><a href="#">SUSISIEKITE</a></li>
                    </ul>
                </div>
            </div>

            {# nav-right #}
            <div class="nav-right">
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <a href="{{ path('logout') }}" class="btn">Atsijungti</a>
                {% else %}
                    <a href="{{ path('security_login') }}" class="btn">Prisijungti</a>
                {% endif %}
            </div>
            {# nav-right #}
        </div>{# container #}
    </nav>{# navbar #}
</header>
